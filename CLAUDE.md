# 프로젝트 지침 · 하루에 게임 1개씩

매일 웹 기반 게임을 하나씩 만드는 저장소. 아래 규칙을 따른다.

## 커밋 규칙

- **Conventional Commits** 형식: `feat(dayNN): ...`, `fix(dayNN): ...`, `chore: ...`, `docs(dayNN): ...`
- **기능 단위로 분리 커밋** — 한 커밋은 한 가지 논리적 변경만 담는다.
- 커밋 메시지 본문은 "무엇을/왜"를 한두 문장으로 설명한다.
- **`Co-Authored-By` 트레일러를 넣지 않는다.** (Claude 등 어시스턴트 co-author 표기 금지)
- 커밋/푸시는 사용자가 요청할 때만 한다.

## 문체 · 이모지

- **이모지를 남발하지 않는다.** 장식·축하용 이모지(🎮 🎯 🚧 ✅ 🎉 🏆 🤖 🎲 등)는 AI가 쓴 티가 나고 산만하다. 문서(README·DEVLOG)와 게임 UI 텍스트 모두 해당.
- 이모지 대신 아래를 쓴다:
  - 구조·강조: 마크다운 헤더(`##`), **굵게**, 구분선(`---`)
  - 상태·목록: 체크박스 `- [ ]` / `- [x]`, 또는 텍스트 라벨 `[예정]` `[적용]` `WIP` `TODO` `DONE` `NEW`
  - 아이콘 대신 텍스트: `vs 봇`, `최고`, `신기록` 등 그냥 단어로
  - 필요할 때만 얌전한 기호: `·` `—` `→` `▸` `»` `※`, 체크가 꼭 필요하면 `✓` `✗`
- 꼭 의미가 필요한 경우가 아니면 이모지 없이 간다.

## 개발 패턴

- **의존성 제로**: 런타임 라이브러리 없이 순수 웹 기술(HTML/CSS/Vanilla JS)만 사용.
- **ES 모듈 + 관심사 분리(OOP)**: 로직·렌더링·입력·상태를 파일 단위로 나눈다.
- **단방향 의존**: 상수 모듈을 최하단에 두고 상위 모듈이 이를 참조. 순환 의존 금지.
- 렌더러는 상태를 **읽기만** 하고 변경하지 않는다.

## 폴더 구조

게임마다 `dayNN-<이름>/` 폴더:

```
dayNN-<이름>/
├── index.html          # 마크업 + 진입점(ES 모듈) 로드
├── css/style.css
├── src/                # 기능별 모듈
│   ├── config.js       # 상수 (single source of truth)
│   ├── ...             # 모델 / 보드 / 렌더러 / 입력 / 게임 루프
│   └── main.js         # DOM 연결 진입점
├── README.md           # 실행법 · 구조 · 조작
└── DEVLOG.md           # 개발 예정 + 개발 적용(Changelog)
```

## 개발 로그

- 게임마다 `DEVLOG.md` 한 파일로 관리한다. `개발 예정(Planned)`과 `개발 적용(Changelog)` 두 섹션.
- 기능을 구현하면 `개발 예정`의 항목을 지우고, `개발 적용`에 **날짜와 함께** 옮긴다.
- 예정/적용을 별도 파일로 쪼개지 않는다.

## 실행

ES 모듈은 `file://`에서 로드되지 않으므로 정적 서버로 실행한다.

```bash
cd dayNN-<이름> && npx serve .
# 또는
python -m http.server 8000
```

## 검증

- 새 게임을 만들면 로컬 서버로 띄워 **콘솔 에러 0**과 핵심 플레이 동작을 실제로 확인한 뒤 커밋한다.
